<Page
    x:Class="Fooxboy.MusicX.Uwp.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fooxboy.MusicX.Uwp.Views"
    xmlns:models="using:Fooxboy.MusicX.Uwp.Models"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="{x:Bind HomeViewModel.Page_Loaded}"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>


        <ScrollViewer Margin="0 0 0 0 " Grid.RowSpan="2">
            <StackPanel Margin="0 32 0 0 " >
                <Grid x:Name="PlaylistGrid"  Grid.Row="0" >
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Margin="30 0 0 0" FontFamily="Calibri" Text="Ваши Плейлисты" Grid.Row="0" FontWeight="ExtraBold" FontSize="42"/>
                    <ListView Grid.Row="1" 
                              ItemsSource="{x:Bind HomeViewModel.Playlists, Mode=TwoWay}"
                              ScrollViewer.VerticalScrollBarVisibility="Disabled"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ScrollViewer.HorizontalScrollMode="Enabled"                  
                              ScrollViewer.VerticalScrollMode="Disabled"
                              x:Name="PlaylistListView"
                              SelectedItem="{x:Bind HomeViewModel.SelectedPlaylistFile, Mode=TwoWay}"
                              ItemClick="{x:Bind HomeViewModel.PlaylistListView_Click}"
                              Tapped="{x:Bind HomeViewModel.PlaylistListView_Tapped}">

                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsStackPanel Background="Transparent"
                                                 Orientation="Horizontal" 
                                                 HorizontalAlignment="Stretch"
                                                 VerticalAlignment="Stretch" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>

                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:PlaylistFile">
                                <Grid Margin="20 0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="auto"/>
                                    </Grid.RowDefinitions>

                                    <controls:DropShadowPanel Width="150"  Height="150" x:Name="AlbumCoverShadow2" ShadowOpacity="1" OffsetY="0" BlurRadius="40" Opacity="0.5">
                                        <Border Width="150" Height="150" Background="Black"/>
                                    </controls:DropShadowPanel>
                                    <Image  Stretch="Fill" Margin="10 20 10 20" Grid.Row="0"  Width="150"  Height="150">
                                        <Image.Source>
                                            <BitmapImage UriSource="{x:Bind Cover, Mode=TwoWay}"
                                                     AutoPlay="True"
                                                     DecodePixelHeight="150"
                                                     DecodePixelWidth="150" />
                                        </Image.Source>
                                    </Image>

                                    <TextBlock HorizontalAlignment="Center" Margin="0 0 0 20" FontWeight="Bold" FontSize="16" Foreground="#282828"  Text="{x:Bind Name}" Grid.Row="1"/>
                                </Grid>
                            </DataTemplate>

                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>

                <Grid x:Name="MusicGrid" Margin="0 20 0 0" Grid.Row="1" HorizontalAlignment="Stretch">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition  Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="30,0,0,0" FontFamily="Calibri" Text="Ваша Музыка" Grid.Row="0" FontWeight="ExtraBold" FontSize="42"/>
                    <TextBlock x:Name="MusicCount" HorizontalAlignment="Left" VerticalAlignment="Bottom" Text="0 треков" Grid.Row="0" Height="19" Margin="10,0,0,7"/>
                    </StackPanel>
                    <ListView ItemContainerStyle="{StaticResource ListViewItemContainerStyle}"
                              Style="{StaticResource ListViewStyle}"
                              SelectedItem="{x:Bind HomeViewModel.SelectedAudioFile, Mode=TwoWay}"
                              ItemClick="{x:Bind HomeViewModel.ListViewMusic_Click}"
                              Tapped="{x:Bind HomeViewModel.MusicListView_Tapped}"
                              Grid.Row="1" HorizontalAlignment="Stretch"
                              ItemsSource="{x:Bind HomeViewModel.Music, Mode=TwoWay}"
                              x:Name="MusicListView">
                        <ListView.ItemsPanel >
                            <ItemsPanelTemplate >
                                <ItemsStackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>

                        <ListView.ItemTemplate >
                            
                            <DataTemplate x:DataType="models:AudioFile">
                                <Grid  Margin="30 0 0 0" >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition MaxWidth="40"/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <controls:DropShadowPanel Grid.Column="0" Margin="1 3 1 1" 
                                                              HorizontalAlignment="Left"
                                                              VerticalAlignment="Center" Width="30"
                                                              Height="30" x:Name="TrackCoverShadow" 
                                                              ShadowOpacity="1" OffsetY="0" 
                                                              BlurRadius="3.5" Opacity="0.5"
                                                              >
                                        <Border Width="30" Height="30" Background="Black"/>
                                    </controls:DropShadowPanel>
                                    <Image HorizontalAlignment="Left"  Margin="1 3 1 1" Stretch="Fill" Width="30" Grid.Column="0"  Height="30">
                                        <Image.Source>
                                            <BitmapImage   UriSource="{x:Bind Cover}"
                                                     DecodePixelHeight="30"
                                                     DecodePixelWidth="30"
                                                     />
                                        </Image.Source>
                                    </Image>
                                    <StackPanel HorizontalAlignment="Left"  Margin="10 0 0 0"  Grid.Column="1">
                                        <TextBlock FontSize="15" Foreground="#282828"  Text="{x:Bind Title}" />
                                        <TextBlock  FontSize="15" Foreground="#9c9c9c" Text="{x:Bind Artist}"/>
                                       
                                    </StackPanel>
                                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="15" Grid.Column="3" Foreground="#282828" Text="{x:Bind DurationMinutes}" Margin="0 0 20 0"/>

                                </Grid>
                            </DataTemplate>

                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </StackPanel>

        </ScrollViewer>

        <Grid x:Name="TitleBarGrid" Background="Transparent" Grid.Row="0"/>

    </Grid>
</Page>
