<Page
    x:Class="Fooxboy.MusicX.Uwp.Views.WelcomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fooxboy.MusicX.Uwp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image  HorizontalAlignment="Center"
                Grid.Row="1" 
                Height="150"
                Width="150"
                Margin="0 0 0 30">
            <Image.Source>
                <BitmapImage DecodePixelHeight="150"
                             DecodePixelWidth="150"
                             UriSource="ms-appx:///Assets/Images/logo.png"/>
            </Image.Source>
        </Image>

       
        
        <TextBlock  HorizontalAlignment="Center"
                    Grid.Row="2"
                    Text="Добро пожаловать в MusicX"
                    FontWeight="Bold"
                    FontSize="32"/>
        
        <TextBlock  HorizontalAlignment="Center"
                    Grid.Row="3" 
                    x:Name="Start"
                    Text="Давайте начнем."
                    FontSize="22"/>

        <ProgressRing Grid.Row="4" 
                      Margin="0 30 0 0"
                      x:Name="ProgressRing"
                      Height="50" Width="50"
                      HorizontalAlignment="Center"
                      IsActive="False"/>

        <Button Grid.Row="4" 
                x:Name="ButtonStart"
                HorizontalAlignment="Center"
                Click="Button_Click"
                Margin="0 50 0 0"
                Content="Начать"/>
    </Grid>
</Page>
