<Page
    x:Class="Fooxboy.MusicX.Uwp.Views.PlaylistView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fooxboy.MusicX.Uwp.Views"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:converters="using:Fooxboy.MusicX.Uwp.Converters" 
    xmlns:my="using:Fooxboy.MusicX.Uwp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource BackgroundApp}">

    <Grid>
        <ScrollViewer>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <controls:DropShadowPanel Grid.ColumnSpan="2" VerticalAlignment="Top" Grid.Column="0"
                                          BlurRadius="50" 
                                          ShadowOpacity="1"
                                           Margin="50"
                                          OffsetX="0" 
                                          OffsetY="0" 
                                              CornerRadius="10"
                                  IsMasked="True">
                    <Border x:Name="ShadowRectangle" Background="White" Height="400" CornerRadius="10"/>

                </controls:DropShadowPanel>

                <Rectangle Grid.ColumnSpan="2" SizeChanged="Rectangle_SizeChanged" VerticalAlignment="Top" Margin="50" RadiusX="10" RadiusY="10" Height="400" Fill="{ThemeResource BlockBackground}"/>


                <Grid Grid.Column="0" Margin="70 70 0 0 " VerticalAlignment="Top" HorizontalAlignment="Left" x:Name="CoverGrid">
                    <controls:DropShadowPanel Grid.Column="0"
                                          BlurRadius="30" 
                                          ShadowOpacity="0.7"
                                          OffsetX="0" 
                                          OffsetY="0" 
                                      x:Name="Shadoww"
                                              CornerRadius="8"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                  IsMasked="True">
                        <Border x:Name="ShadowCover" Background="White" Height="250" Width="250" CornerRadius="8"/>

                    </controls:DropShadowPanel>

                    <controls:ImageEx x:Name="CoverImage" Height="250" Width="250" 
                                HorizontalAlignment="Center"
                                DecodePixelHeight="250"
                                DecodePixelWidth="250"
                                CornerRadius="8"
                                 Source="{x:Bind PlayerViewModel.Cover, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Stretch="Fill"
                                 PlaceholderStretch="Fill"
                                IsCacheEnabled="False"
                                PlaceholderSource="ms-appx:///Assets/Images/placeholder-album.jpg" />
                </Grid>

                <Grid Margin="20 70" x:Name="InfoPlaylistGrid" Grid.Column="1" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <TextBlock FontSize="30" FontWeight="ExtraBlack" Text="Title Album" Grid.Row="0"/>
                    <TextBlock FontSize="23" Foreground="{ThemeResource ColorArtistText}" Text="Artist Album" Grid.Row="1"/>
                </Grid>

            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
