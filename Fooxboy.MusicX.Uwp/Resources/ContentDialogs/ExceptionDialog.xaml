<ContentDialog
    x:Class="Fooxboy.MusicX.Uwp.Resources.ContentDialogs.ExceptionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fooxboy.MusicX.Uwp.Resources.ContentDialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Произошла ошибка"
    PrimaryButtonText="ОК"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">
    <Grid>

        <StackPanel>
            <TextBlock Text="{x:Bind TitleText}" TextWrapping="Wrap" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="{x:Bind Info}" TextWrapping="Wrap"/>
            <TextBlock Text="{x:Bind ExceptionString}" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</ContentDialog>
